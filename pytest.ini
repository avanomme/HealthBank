[pytest]
python_files = test_*.py
testpaths = backend/tests
norecursedirs = frontend node_modules .git __pycache__ .venv venv integration stress
addopts = -p no:cacheprovider --tb=short -q
markers =
    integration: marks tests that require a live API/Docker stack (deselect with -m "not integration")
